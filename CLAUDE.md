# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

LOTHardware ("Living Off The Hardware") is a Hugo static site that catalogs malicious and attack capable USB and HID hardware (Rubber Ducky, Digispark, Flipper Zero, Logitech Unifying, and so on) for defenders. Each device page documents its USB identifiers (VID/PID, device instance path, class) plus detection notes so blue teams can build AV and EDR blocking rules. Live at https://lothardware.com.tr/.

## Prerequisites and commands

Hugo is the only build tool. There is no package.json, Gemfile, Makefile, or CI workflow in the repo. The site config is `config.yml` (legacy filename, not `hugo.toml`).

Two things commonly block a first build:

1. Hugo may not be installed in the current environment. Install Hugo (extended is recommended, because the theme builds `search.js` through Hugo Pipes and ESBuild).
2. The theme is a git submodule. If `themes/hugo-index` is empty, fetch it first:

   ```
   git submodule update --init --recursive
   ```

   Theme source: https://github.com/adityatelange/hugo-index

Common commands:

```
hugo server -D               # dev server, includes drafts
hugo --minify                # production build into public/
hugo new content/<slug>.md   # new device page from archetypes/default.md
```

## Architecture

- One Markdown file per device lives in `content/*.md`. There is no project level `layouts/` directory, so all templates come from the theme submodule. Override a template by recreating its path under a project level `layouts/` folder instead of editing the submodule.
- Front matter (YAML): `title`, `date`, `tags` (array of strings), `description`. The archetype sets `draft: true`, so a new page stays hidden until you set `draft: false` or build with `-D`.
- The body follows a fixed section template. Use `content/hak5-rubber-ducky.md` as the canonical example. The page opens with a centered `<img src="/images/<slug>.jpg">` block, then `## Description`, then these `###` sections in order: Device Instance Path, VendorID, ProductID, Class, Author, Sigma/Yara Rules, Links. Some older pages also carry a Limitations section.
- Device photos live in `static/images/` and are referenced from the site root, for example `/images/teensy.jpg`. Every live page currently has a matching image.
- Taxonomy: a single `tag` taxonomy. A custom permalink puts tag pages at `/@<slug>` (`permalinks.tags: "@:slug"`).
- Markdown allows raw HTML (`goldmark.renderer.unsafe: true`), which is how the centered image blocks work. Code highlighting uses CSS classes (`pygmentsUseClasses`) styled by `assets/chromastyles.css` (regenerate with `hugo gen chromastyles`).

### How assets and search are wired

The theme partial `themes/hugo-index/layouts/partials/head.html` drives this:

- `assets/styles.css` and `assets/chromastyles.css` are minified and concatenated into `/assets/stylesheet.css`. The `assets/` copies are the source of truth. There used to be a `static/styles.css` that was an exact, unreferenced duplicate, and it was removed.
- Search is client side with Fuse.js. `config.yml` adds `JSON` to the home output, so Hugo renders `layouts/index.json` into `/index.json`. `assets/search.js` fetches it and searches over `title` and `tags.title`. Fuse.js is pulled at build time and bundled, so there is no runtime CDN call.
- The theme also bundles `quicklink` to prefetch in viewport links, which speeds up navigation.

## hardwares.json

`hardwares.json` (repo root) is a hand maintained, machine readable export of the catalog. The homepage "download JSON" link points at this file. It is not generated by Hugo. When you add, edit, or remove a device you must also update its `products` entry (`title`, `date`, `instancePath`, `author`) and keep it in sync with the matching content page.

## Catalog status and roadmap

These pages exist but still hold placeholder bodies ("Coming soon." description and empty value blocks): `arduino-pro-micro`, `digispark-attiny85`, `lilypad-attiny85`, `teensy`, `usb-nova`. They are the next thing to fill in. The plan is to grow the catalog, both these stubs and brand new devices, by reviewing security literature and GitHub projects. Do not invent VID/PID values or descriptions. Use sourced data only.

### Improvement backlog (speed and design)

- Replace the raw `<img width="50%">` tags with Hugo image processing (page bundles plus the `images` functions) to emit real width and height, WebP, and a responsive `srcset`. Add `loading="lazy"` and `decoding="async"`.
- Show device thumbnails and a short blurb on the index instead of a plain text table, for example a responsive card or grid layout, by overriding `layouts/index.html` and `partials/table-list.html` at project level.
- Add a dark mode toggle and a small accent color system for a more modern look.
- Add OpenGraph and Twitter card meta plus a default share image so links look good when shared.
- Fingerprint the bundled CSS and JS (`| fingerprint`) for long lived caching.
- Compress the source JPG and PNG files in `static/images/`, since several are larger than they need to be.

## Working conventions

- Commit messages: write them in English, in plain human style. Use a short imperative subject line and add a brief body only when it helps explain the why. Do not add AI attribution, "Generated with" lines, or Co-Authored-By trailers, and do not write robotic or padded text.
- Punctuation: do not use em dashes anywhere, in commits, content, or docs. Prefer commas, periods, parentheses, or a plain hyphen.
- The site cannot always be built or rendered locally here (Hugo may be missing), so changes are often made by static reasoning. Say so when a change would benefit from a real render check.
- Work on a branch and let the repo owner review before anything lands on `main`.
